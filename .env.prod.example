# Copy to .env.prod and fill in real values before running docker-compose.
# Generate SECRET_KEY_BASE with: docker run --rm elixir:1.17-slim mix phx.gen.secret

SECRET_KEY_BASE=replace_me_generate_with_mix_phx_gen_secret
PHX_HOST=reibisch.de
PORT=4000
