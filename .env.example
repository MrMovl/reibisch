# Required in production
# Generate with: mix phx.gen.secret
SECRET_KEY_BASE=

# Your production domain (no scheme, no trailing slash)
PHX_HOST=example.com

# HTTP port the app listens on inside the container (default: 4000)
PORT=4000
